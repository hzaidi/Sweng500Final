(function () {'use strict';
	describe('kpi (service)', function () {
		var service;
		beforeEach(function () {
			inject(function (_kpiSvc_) {
				service = _kpiSvc_;
			});
		});
		// #--------------------------------------# //
		// #----- Service Unit Test (kpiSvc) -----# //

		// jasmine tests for service functions
		it('your Test', function () {

		});

		// #--- END Service Unit Test (kpiSvc) ---# //
		// #--------------------------------------# //
	});
}());
