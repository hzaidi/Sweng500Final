(function () {'use strict';
	describe('{{sentenceCase name}} (service)', function () {
		var service;
		beforeEach(function () {
			inject(function (_yourSvc_) {
				service = _yourSvc_;
			});
		});
		// #-------------------------{{dashes (camelCase name)}}-----------# //
		// #----- Service Unit Test ({{camelCase name}}Svc) -----# //

		// jasmine tests for service functions
		it('your Test', function () {

		});

		// #--- END Service Unit Test ({{camelCase name}}Svc) ---# //
		// #---------------------------{{dashes (camelCase name)}}---------# //
	});
}());
