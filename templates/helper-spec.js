(function () {'use strict';
	describe('{{sentenceCase name}} (helper)', function () {
		var helper;
		beforeEach(function () {
			inject(function (_yourHelper_) {
				helper = _yourHelper_;
			});
		});
		// #-------------------------{{dashes (camelCase name)}}-----------# //
		// #----- Service Unit Test ({{camelCase name}}Svc) -----# //

		// jasmine tests for helper functions
		it('your Test', function () {

		});			

		// #--- END Service Unit Test ({{camelCase name}}Svc) ---# //
		// #---------------------------{{dashes (camelCase name)}}---------# //
	});
}());
