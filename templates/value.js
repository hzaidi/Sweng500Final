(function() {'use strict';var app = angular.module('piStatus');

	// #-------------{{dashes (camelCase name)}}-----------# //
	// #----- Value ({{camelCase name}}Val) -----# //
	app.value('{{camelCase name}}Val', {some: 'value'});
	// #--- END Value ({{camelCase name}}Val) ---# //
	// #---------------{{dashes (camelCase name)}}---------# //

}());
